% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decimal.R
\name{summary.decimal}
\alias{summary.decimal}
\title{Summarise a decimal vector}
\usage{
\method{summary}{decimal}(object, ..., maxsum = 100L, digits = NULL)
}
\arguments{
\item{object}{A \code{decimal} vector.}

\item{...}{These dots must be empty.}

\item{maxsum, digits}{Accepted for compatibility with \code{\link[=summary.data.frame]{summary.data.frame()}},
which passes them to every column, and ignored. \code{digits} in particular is
not honoured: rounding an exact decimal for display is the surprise this
package exists to avoid.}
}
\value{
A named \code{decimal} vector holding \code{Min.}, \verb{1st Qu.}, \code{Median},
\code{Mean}, \verb{3rd Qu.} and \code{Max.}, followed by \verb{NA's} when the input contains
missing values.
}
\description{
The six-number summary that \code{\link[=summary]{summary()}} gives for a numeric vector, computed
in exact decimal arithmetic and returned as a \code{decimal} vector rather than a
vector of doubles.
}
\details{
Missing values are removed before the statistics are computed and reported
as an \verb{NA's} entry, matching \code{\link[=summary.default]{summary.default()}}. As in base R, \code{NaN} counts
as missing here, because \code{\link[=is.na]{is.na()}} is true for it.

The quartiles use the type 7 definition, the default of
\code{\link[stats:quantile]{stats::quantile()}}. A quantile that falls between two elements is
interpolated, and the mean divides by the number of elements, so both run
under the active decimal context and may raise \code{inexact} and \code{rounded}
signals like any other division. The minimum, the maximum, and any quantile
that lands exactly on an element are always exact.

Because a \code{decimal} vector carries one shared scale, every entry is padded to
the widest one present -- including the \verb{NA's} count, which is a count rather
than a measured value. Interpolating a quartile can need more digits than the
input carries, which widens that shared scale.

Interpolating between \code{-Infinity} and \code{Infinity} is an invalid operation, and
the default context traps it, so summarising a vector that spans both signed
infinities raises an error rather than returning \code{NaN} quartiles. That is the
same error \code{decimal("Infinity") - decimal("Infinity")} raises. Clear the trap
with \code{\link[=with_decimal_context]{with_decimal_context()}} to get base R's \code{NaN} instead.
}
\examples{
summary(decimal(c("1.25", "2.50", "3.75", "10.00")))

# Missing values are counted, not propagated.
summary(decimal(c("1.5", NA, "2.5")))
}
