% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/arrow.R
\name{arrow_decimal_type}
\alias{arrow_decimal_type}
\title{Arrow type for a decimal column}
\usage{
arrow_decimal_type(precision, scale = 0L)
}
\arguments{
\item{precision}{Total number of digits, at most 76.}

\item{scale}{Number of fractional digits.}
}
\value{
An Arrow extension type.
}
\description{
Builds the Arrow extension type this package writes \code{decimal} vectors as,
with a chosen precision and scale rather than ones inferred from the
values. Use it to pin the type of a column that will be appended to, for
example through the \code{schema} argument of \code{arrow::arrow_table()} or the
\code{type} argument of \code{arrow::as_arrow_array()}. The storage is
\code{arrow::decimal128()} up to 38 digits of precision and
\code{arrow::decimal256()} above that.
}
\examples{
if (requireNamespace("arrow", quietly = TRUE)) {
  arrow_decimal_type(20, 2)
  arrow::as_arrow_array(decimal("1.25"), type = arrow_decimal_type(20, 2))
}
}
\seealso{
\link{decimal_arrow} for how the type behaves.
}
