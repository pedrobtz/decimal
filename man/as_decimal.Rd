% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/arrow.R, R/decimal.R
\name{as_decimal.Array}
\alias{as_decimal.Array}
\alias{as_decimal.ChunkedArray}
\alias{as_decimal}
\alias{as_decimal.decimal}
\alias{as_decimal.character}
\alias{as_decimal.integer}
\alias{as_decimal.numeric}
\alias{as_decimal.default}
\title{Convert a vector to decimal}
\usage{
\method{as_decimal}{Array}(x, scale = NULL)

\method{as_decimal}{ChunkedArray}(x, scale = NULL)

as_decimal(x, scale = NULL)

\method{as_decimal}{decimal}(x, scale = NULL)

\method{as_decimal}{character}(x, scale = NULL)

\method{as_decimal}{integer}(x, scale = NULL)

\method{as_decimal}{numeric}(x, scale = NULL)

\method{as_decimal}{default}(x, scale = NULL)
}
\arguments{
\item{x}{A decimal, character, integer, or double vector, or an Arrow
\code{Array} or \code{ChunkedArray}.}

\item{scale}{An integer scalar giving the number of fractional digits to
store, or \code{NULL} to use the global default or input-specific inference.}
}
\value{
A \code{decimal} vector.
}
\description{
\code{as_decimal()} is the conversion generic for decimal vectors. Character
values are parsed exactly, integer values are converted exactly, and
existing decimal vectors are returned or rescaled. Promotion to a finer
shared scale is exact and context-free; reduction to a coarser scale is
quantized. Double values are decoded from their exact IEEE 754
representation and therefore require an explicit or globally configured
scale before quantization.
}
\details{
When \code{scale} is \code{NULL}, \code{getOption("decimal.default_scale")} is used when
set. Without that option, character input uses the largest number of
fractional digits found in the input, integer input uses scale zero, and
double input raises an error. Quantization uses the active
\code{\link[=decimal_context]{decimal_context()}}.

An Arrow \code{Array} or \code{ChunkedArray} of any decimal type converts exactly,
taking its scale from the Arrow type rather than from the values, so a
chunk holding only whole numbers keeps its declared fractional digits. An
Arrow integer array converts exactly at every width. Any other Arrow type
converts to an R vector first and then follows the rules above. See
\link{decimal_arrow} and \code{vignette("arrow-decimal-types")}.
}
\examples{
as_decimal(c("1.2", "3.45"))
}
