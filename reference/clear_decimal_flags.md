# Clear sticky decimal flags

Clear sticky decimal flags

## Usage

``` r
clear_decimal_flags()
```

## Value

The previously active sticky flags, invisibly.

## Examples

``` r
clear_decimal_flags()
```
