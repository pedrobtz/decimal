# Report the bundled mpdecimal runtime version

Returns the version string reported by the bundled `mpdecimal` library
that was loaded with the package DLL.

## Usage

``` r
mpdecimal_version()
```

## Value

A length-one character vector.

## Examples

``` r
mpdecimal_version()
#> [1] "4.0.1"
```
